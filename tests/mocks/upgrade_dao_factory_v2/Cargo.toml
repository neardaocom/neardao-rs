[package]
name = "upgrade_dao_factory_v2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
near-sdk = {version = "4.0.0", features = ["unstable"]}

[features]
testnet = []
