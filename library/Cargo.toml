[package]
name = "library"
version = "0.1.0"
edition = "2018"

#[lib]
#crate-type = ["cdylib"] # enable rlib for simulations tests, remove in prod

[dependencies]
near-sdk = "3.1.0"

[features]
testnet = []